import React from 'react';
// Tidak menggunakan kata 'function', tapi menggunakan tanda panah =>
const HelloWorld = () => {
    return (
        <div>
            <h1>Halo World!</h1>
        </div>
    );
};

export default HelloWorld;
